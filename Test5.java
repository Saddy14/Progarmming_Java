// import java.util.ArrayList;
// import java.util.Arrays;

public class Test5 {
    public static void main(String[] args) {
        
        // String name = "Bro";
        // int num = 5;
        
        // boolean result = name.equalsIgnoreCase("bro"); -true
        // boolean result = name.equals("bro");           -true
        // int result = name.length();                    -3
        // char result = name.charAt(0);                  -B
        // int result = name.indexOf("B");                -returns index of the given string
        // boolean result = name.isEmpty();              //check if the string is empty returns boolean value
        // name = name.toUpperCase();
        // name = name.toLowerCase();
        // name = name.trim();                          //remove empty space b4 and after
        // String result = name.replace('o', 'a');      //replace char

        // System.out.println(result/name);




    }
}
